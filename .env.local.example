# ===================================
# OnTheDeal 환경 설정
# 도메인: onthedeal.com
# ===================================

# 앱 URL (프로덕션: https://onthedeal.com)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# NextAuth 설정
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-change-in-production-min-32-chars

# Database (Prisma)
DATABASE_URL=your-database-url

# 토스페이먼츠 설정
# https://developers.tosspayments.com 에서 발급받은 키를 입력하세요
# 테스트용 키는 test_ 로 시작합니다

NEXT_PUBLIC_TOSS_CLIENT_KEY=test_ck_xxx  # 클라이언트 키 (결제창 호출용)
TOSS_SECRET_KEY=test_sk_xxx              # 시크릿 키 (서버 API 호출용)

# 이 파일을 .env.local로 복사하여 사용하세요
# cp .env.local.example .env.local
